pragma solidity ^0.4.18;

contract MyToken {

  // event ErrorLog(string message);
  // event FooLog(address sender, address beneficiary, uint256 value);

  // function transfer(address _to, uint256 _value) public returns (bool) {
  //   // emit ErrorLog("hello from mytoken");

  //   // if (_to == address(0)) {
  //   //   emit ErrorLog("address is 0");
  //   //   return false;
  //   // }

  //   // if (_value > balances[msg.sender]) {
  //   //   emit ErrorLog("insufficient token balance to transfer");
  //   //   return false;
  //   // }

  //   // require(_to != address(0));
  //   // require(_value <= balances[msg.sender]);

  //   // balances[msg.sender] = balances[msg.sender].sub(_value);
  //   // balances[_to] = balances[_to].add(_value);
  //   // emit Transfer(msg.sender, _to, _value);
  //   return true;
  // }

  function bar() public returns (bool) {
    return false;
  }

  // function foo(address _beneficiary, uint256 _value) public returns (bool) {
  //   emit ErrorLog("hello from mytoken");
  //   emit FooLog(msg.sender, _beneficiary, _value);

  //   return true;

  //   // if (_to == address(0)) {
  //   //   emit ErrorLog("address is 0");
  //   //   return false;
  //   // }

  //   // if (_value > balances[msg.sender]) {
  //   //   emit ErrorLog("insufficient token balance to transfer");
  //   //   return false;
  //   // }

  //   // require(_to != address(0));
  //   // require(_value <= balances[msg.sender]);

  //   // balances[msg.sender] = balances[msg.sender].sub(_value);
  //   // balances[_to] = balances[_to].add(_value);
  //   // emit Transfer(msg.sender, _to, _value);
  // }

}
